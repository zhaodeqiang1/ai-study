# SQL 迁移契约

每个 SQL/migration 必须满足本契约。

## 必须包含

- 迁移编号。
- 关联需求编号。
- 正向 SQL。
- 回滚 SQL 或明确回滚方案。
- 数据影响说明。
- 部署顺序。
- 验证 SQL。
- 不可逆说明。

## 不可逆操作

如果迁移不可逆，必须说明：

- 为什么不可逆。
- 是否需要备份。
- 是否需要停机窗口。
- 谁确认可以执行。
- 如何降低风险。

## 禁止

- 只有正向 SQL。
- 无需求编号的生产 SQL。
- 无说明删除表、删除字段、清空数据。
- 把生产数据修复 SQL 混入初始化 SQL。

